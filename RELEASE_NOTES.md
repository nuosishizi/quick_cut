# 快剪 QuickCut 2.7.46

本版在保留 2.7.39 恢复版“AI 语音隔离”默认声音与操作不变的前提下，新增独立的“高级音频修复”效果架。用户可以像在达芬奇 Fairlight 中一样，按顺序叠加片段效果和音轨效果；核心使用随软件提供的 FFmpeg、RNNoise 和开放算法实现，不使用或冒充 Blackmagic 私有神经模型。

## 高级音频修复效果架

- 支持“片段效果 / 音轨效果”两个作用域；片段 FX 先处理，音轨 FX 后处理。
- 每个片段或音轨最多 6 个效果插槽，可启用/旁路、上下排序、复制、删除和重置，操作支持工程保存与撤销。
- 新增 8 类处理：人声隔离、噪声降低、De-Hummer 电流嗡声消除、对话分离、De-Esser 去齿音、扩展器/噪声门、六段参数均衡器、压缩器/限幅器。
- 原有快速降噪仍保持为独立入口，没有更改默认 85% 的 2.7.39 恢复链，也没有改动字幕、剪辑、调色、美颜等其他功能。

## Noise Learn 与自动去嗡声

- “噪声降低”可从时间线选区学习纯噪声；无选区时从播放头学习 2 秒。
- 学习结果保存 15 段噪声频谱、噪声底、学习位置和时长，重开工程后仍然保留。
- 独立音频片段会读取自己的音频文件进行学习和试听，不会误用主视频声音。
- De-Hummer 可手动选择 50 Hz / 60 Hz，也可自动分析基频与谐波；检测不可靠时安全旁路，避免误伤低频人声。

## 试听、缓存与导出

- 新增高级效果 A/B 试听；靠近素材结尾时会自动调整试听窗口，不会直接跳到文件末尾。
- 可生成整轨高级音频缓存，时间线播放与最终导出复用同一份结果。
- 片段 FX 在剪切和拼接前应用，音轨 FX 在无缝拼接后应用；最终音频仍使用样本级 `concat`，没有改回会造成重音的 `amix` 拼接。
- 已预渲染的音轨 FX 在导出时不会再次处理；片段 FX 仍会正确保留，避免双重降噪、音色变薄或缓存失效。
- 所有效果都接入视频和 MP3 导出；效果开启后自动退出无处理智能复制，确保听到的结果真正写入成片。

## 开放技术与兼容性

- 人声隔离使用随包提供并附带许可证的 RNNoise 模型，加轻量频谱残留清理。
- 对话分离优先使用 FFmpeg 开放的对话提取通道与中/侧声道重混；旧版 FFmpeg 缺少该滤镜时自动回退为兼容的中/侧声道聚焦，不会阻断导出。
- Windows 与 macOS 打包继续携带完整 FFmpeg/FFprobe 运行库；macOS 构建会收集动态库、重写依赖路径并签名，避免只有外壳而无法渲染。

## 验证

- 使用真实 FFmpeg 完成 15 段 Noise Learn、完整 6 插槽处理、自动 50/60 Hz 分析、A/B 试听和整轨缓存验证。
- 使用真实视频验证“预渲染音轨 FX 不重复、片段 FX 不丢失”，导出 AAC 音轨可播放且时长保持同步。
- 合成风声、工频嗡声和高频虫鸣测试中，静音段噪声均方电平下降约 4.2 dB，含声段总体电平变化约 1 dB。
- 浏览器实测完成插槽上限、参数调节、Noise Learn、排序、旁路、复制、删除、A/B、外部音频和工程重开持久化；控制台 0 错误。
- 完整自动化回归：305 项，300 项通过，5 项仅因当前平台不是对应打包系统而跳过，0 项失败。
- Windows 安装包和便携包已核对包含新模块、RNNoise 模型、许可证与全部新增 FFmpeg 滤镜。

## 下载文件

- Windows 安装版：`QuickCut-Windows-2.7.46-Setup.exe`
- Windows 便携版：`QuickCut-Windows-2.7.46-Portable.zip`
- macOS 安装版：`QuickCut-macOS-2.7.46-Installer.dmg`
- macOS 便携版：`QuickCut-macOS-2.7.46-Portable.zip`
