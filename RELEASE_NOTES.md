# 快剪 QuickCut 2.7.47

## 🎛️ 专业多级人声降噪工作台（全新架构）

- **50/60 Hz 工频嗡声自动检测与 De-Hum**：自动探测基频并消除至第 6 谐波，支持手动强制开关与深度调节。
- **有序降噪链**：风噪/低频轰鸣 → 持续环境声 → RNNoise 神经人声净化 → 蛐蛐等高频虫鸣 → 温和残留清理 → EQ/人声清晰度/齿音控制/限幅，全部阶段独立可调。
- **6 大噪声场景预设**：均衡、风噪、电流嗡声、飞机/空调、虫鸣、重度混合 —— 一键切换适合当前环境的降噪参数。
- **7 段可视化降噪调节界面**：每一级降噪阶段在 UI 上独立展示，可实时调整深度与开关。
- **试听 ↔ 整轨 ↔ 时间线 ↔ 导出统一参数**：降噪配置全链路共享同一套参数，缓存签名一致，避免预览与导出不一致。

## 🎚️ 专业音效处理器 (Audio FX Rack)

- **新增 audio-fx 模块**：支持人声隔离、噪声降低、电流嗡声消除、对话分离、去齿音、扩展器/噪声门、六段参数均衡器、压缩器/限幅器共 8 种效果类型。
- **可视化效果机架 UI**：支持效果插槽拖拽排序、单独绕过、参数调节，最多 6 个效果槽位。
- **噪声学习采样**：可从音频片段自动学习噪声频谱特征，用于精准频谱减法降噪。
- **实时预览与整轨渲染**：Audio FX 支持片段预览试听和全轨道一次性渲染。

## 📝 字幕连续对齐修复

- 修复语音识别偶发漏词后，字幕重新排版会在同一句中产生空白片段的问题。
- 以实际工程 `Proverbs 12:1 says, "Whoever hates correction is stupid."` 的毫秒时间为回归样本：`Whoever` 与 `hates` 之间不再断屏。
- 初次生成字幕、切换一行/多行、调整字幕宽度和重新排版现在统一使用同一套连续性校准逻辑。
- 短句内的安全间隔由上一张字幕持续显示至下一张开始；逐词时间戳保持不变，不影响逐词高亮。
- 不跨越句号、重复阅读、读错、待删除或人工复核片段，避免把不应连接的字幕误连。

## 🔧 其他改进

- CI/CD 工作流增加 Build Provenance Attestation（SLSA 供应链安全签名），确保安装包可验证来源。
- Windows 打包增加完整独立 FFmpeg/FFprobe 校验，验证 `afftdn`、`arnndn`、`crystalizer`、`deesser` 四个音频滤镜在包内可用，并执行 AAC 实际渲染冒烟测试。
- macOS 打包支持 Homebrew FFmpeg 动态库自动收集与 `install_name_tool` 重写，生成完全自包含的 Apple Silicon 便携包。
- 安装包 README 同步更新使用说明与测试建议。

## 🧪 识别与验证

- 使用实际工程原视频重新提取音频并复跑 Deepgram 词级识别，核对 `verse / one / says / Whoever / hates` 的真实顺序与时间边界。
- 新增真实毫秒回归测试，覆盖识别缺词、文稿补词、单行字幕重新排版后的连续显示。
- 新增专业降噪链单元测试：真实 FFmpeg 音轨渲染、工频识别、降噪效果与音画时长校验。
- 新增 Audio FX 模块测试：效果链构建、噪声特征学习、参数规范化。
- 全量自动化测试通过；视频导出、音频渲染、降噪、达芬奇输出和时间线编辑测试均无退化。

## 📦 完整安装包

- Windows 安装版：`QuickCut-Windows-2.7.47-Setup.exe`
- Windows 便携版：`QuickCut-Windows-2.7.47-Portable.zip`
- macOS 安装版：`QuickCut-macOS-2.7.47-Installer.dmg`
- macOS 便携版：`QuickCut-macOS-2.7.47-Portable.zip`

Windows 包强制校验完整独立 FFmpeg/FFprobe 并执行 AAC 渲染冒烟测试；macOS 包由 GitHub macOS 构建机生成完整可安装 DMG 和便携 ZIP，附带一键解除 Gatekeeper 隔离工具。
