# 快剪 QuickCut 2.7.46

本版根据实际试听反馈，恢复 2.7.39 中自然、稳定的“AI 语音隔离（达芬奇级·强力去杂音）”，撤下容易重复削减人声频谱的七级串联默认方案。

## 恢复达芬奇级 AI 语音隔离

- 默认恢复为 2.7.39 的固定处理顺序：48 kHz 重采样 → RNNoise 神经人声净化 → 一次频谱降噪 → 轻量非局部均值清理 → 峰值限幅。
- 默认净化强度恢复为 85%，优先减少杂音，同时保留人声的自然度、空气感和清晰度。
- “AI 语音隔离（达芬奇级·强力去杂音）”重新成为默认入口和默认处理模式。
- 已保存为“专业多级降噪”的现有工程会自动迁移到恢复后的 AI 语音隔离，并清除旧的多级降噪缓存，避免继续播放失真的旧缓存。
- 录音棚人声母带、Demucs、去回声、温和与强力模式仍可按需要手动选择。

## 预览、缓存与导出

- 试听、应用整条音轨和视频导出使用同一套恢复后的参数，避免“试听正常、导出不同”。
- 模式或强度变化会使旧降噪音轨失效并重新生成。
- 整轨输出继续采用临时文件原子写入，失败时清理残留并使用稳定回退链，防止工程引用半成品。

## 验证

- 使用真实 FFmpeg 对带粉红噪声的模拟人声执行恢复后的整轨链路，成功生成可播放 M4A。
- 5 秒输入输出仍为 5.000 秒，音画时序没有漂移。
- Windows 安装包和便携包均已核对包含恢复后的界面、滤镜代码、RNNoise 模型和 FFmpeg 运行库。
- 完整自动化回归：299 项，294 项通过，5 项因当前平台缺少打包运行时而跳过，0 项失败。

## 下载文件

- Windows 安装版：`QuickCut-Windows-2.7.46-Setup.exe`
- Windows 便携版：`QuickCut-Windows-2.7.46-Portable.zip`
- macOS 安装版：`QuickCut-macOS-2.7.46-Installer.dmg`
- macOS 测试包：`QuickCut-macOS-2.7.46-Portable.zip`
