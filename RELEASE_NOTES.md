# 快剪 QuickCut 2.7.45

本版集中完成时间线、字幕校对、工程管理、预览与导出稳定性升级。

## 主要更新

- 时间线空白区域支持选中状态与波纹删除，交互更接近达芬奇。
- `Alt+A` 可选中播放头之后的全部轨道素材；跨越播放头的素材会先安全分割，再整体后移。
- 支持批量调整多个视频片段的缩放、位置和其他画面参数。
- 新增全屏预览；工程首页按日期分组，方便查找每天的工程。
- 字幕默认单行，同时保留双行和多行重排，切换时无需重新识别。
- 红色口播差异可直接用真实识别文字替换错误文稿，保留原时间戳。
- 新增“英文主字幕 + 中文辅助翻译”校对视图：中文可编辑、可人工锁定，并在英文变化时提示更新；中文仅供查看，不进入视频或达芬奇导出。
- “中文原声识别”与“英文字幕的中文辅助翻译”分离，避免功能混淆。
- 新增显卡编码重新检测，显示通过真实编码测试的硬件结果。
- 合并调色、滤镜、美颜与高级画面参数，补齐阴影、高光、黑白场、褪色和锐化的导出渲染。
- Windows 降噪避开已确认会崩溃的 `anlmdn` 路径；高级滤镜失败时自动回退稳定降噪链。
- Demucs 不再把清单文件伪装成可执行引擎，只有真实可运行的本地引擎才显示可用。

## 验证

- 完整自动化回归：296 项，291 项通过，5 项因当前平台缺少打包运行时而跳过，0 项失败。
- 字幕、时间线、降噪试听、整轨降噪、视频导出、达芬奇发送与文件导出均保留回归覆盖。
