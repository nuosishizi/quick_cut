# 快剪 QuickCut 2.7.46

本版把原有单一强度降噪升级为可分析、可分层调节、可试听并可整轨应用的专业人声降噪链，重点处理风声、飞机/空调等持续环境声、电流嗡声和蛐蛐等高频残留。

## 专业多级降噪

- 新增 7 段有序处理：De-Hum 电流嗡声 → 风噪/低频轰鸣 → 持续环境声 → RNNoise 神经人声净化 → 高频虫鸣/嘶声 → 温和残留清理 → EQ 人声修复与限幅。
- 新增真实 50/60 Hz 工频分析。自动模式只有在检测可靠时才处理基频与谐波，避免无电流声时强行削弱人声；也可手动强制 50 Hz 或 60 Hz。
- 第二次频谱降噪使用更温和且不同的参数，不再盲目叠加完全相同的 Noise Reduction，降低金属声和水声伪影风险。
- 新增均衡人声、户外风声、电流嗡声、飞机/空调、蛐蛐/高频声、混合重噪声 6 个场景预设。
- 每一层都可独立调节，界面同时提示过度处理可能带来的音质损失。
- 保留录音棚净化、DeepFilterNet、Demucs、去回声、温和和强力模式作为兼容选项；外部引擎只有真实可执行时才启用。

## 预览、缓存与导出

- 试听、应用整条音轨和视频导出使用同一套专业参数，避免“试听正常、导出不同”。
- 全部阶段参数都进入缓存签名；任何一层变化都会使旧降噪音轨失效并重新生成。
- 整轨输出继续采用临时文件原子写入，失败时清理残留并使用稳定回退链，防止工程引用半成品。
- Windows 专业链继续避开已确认不稳定的 `anlmdn` 路径。

## 验证

- 合成视频实测同时包含 50 Hz/100 Hz 电流嗡声、7.2 kHz 高频声、粉红噪声与模拟人声：能正确识别 50 Hz，处理后嗡声显著下降。
- 实际生成 M4A 音轨可播放、文件有效，音频时长误差小于 0.08 秒，没有残留 `.partial` 文件。
- 浏览器真实界面验收通过：预设联动、专业/兼容模式切换和参数禁用状态均正常。
- 完整自动化回归：299 项，294 项通过，5 项因当前平台缺少打包运行时而跳过，0 项失败。

## 下载文件

- Windows 安装版：`QuickCut-Windows-2.7.46-Setup.exe`
- Windows 便携版：`QuickCut-Windows-2.7.46-Portable.zip`
- macOS 安装版：`QuickCut-macOS-2.7.46-Installer.dmg`
- macOS 测试包：`QuickCut-macOS-2.7.46-Portable.zip`
