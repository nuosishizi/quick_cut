# 快剪 Final 构建说明

编辑器本体是 `Modules/EditorRuntime/editor-desktop`，Windows 和 macOS 共用同一套界面与达芬奇导出。

## 发给别人测试

- Windows：运行 `pack-windows.ps1`，桌面得到 `快剪-Windows-2.7.39-测试包.zip`。对方解压后双击 `启动快剪.bat`。
- macOS Apple 芯片：运行 `pack-macos.ps1`，桌面得到 `快剪-macOS-2.7.39-测试包.zip`。对方解压后右键 `启动快剪.command` 选择「打开」。
- 两份测试包都不含 API Key、工程和素材。

## 在本机开发

- Windows：双击 `启动快剪.bat`。打包后的测试包已内置 Node / FFmpeg；开发目录仍可用系统里的 Node / FFmpeg。
- macOS：双击 `启动快剪.command`（免 Xcode，用包内 Bun 打开浏览器）。若要原生窗口，在 Mac 上运行 `一键生成APP.command`，用 Xcode 编译 SwiftUI 壳并嵌入 Bun / FFmpeg，输出桌面 `快剪.app`。当前正式包不包含 Chromium / CEF。

Google Sheets 链接识别不依赖页面 DOM：当前 Sheet 使用官方 CSV 导出地址读取，因此右侧行号来自实际 CSV 行序；网页区域只负责浏览。这避免 Google Sheets 页面内部 DOM 变化导致“识别链接”按钮失效。
