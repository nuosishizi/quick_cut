# 快剪 Final 构建说明

macOS：一键脚本编译 SwiftUI 壳，用 `WKWebView` 嵌入本地编辑器，并打包 Bun / FFmpeg。当前正式包不包含 Chromium / CEF。

Windows：不编译 Xcode 工程。安装 Node.js 与 FFmpeg 后，运行根目录 `启动快剪.bat`。编辑器仍是 `editor-desktop`，由 Node 起本地 HTTP，再用 Edge `--app` 窗口打开。

一键脚本会：检查 Xcode/Python → 准备便携 CMake → 从 `cef-builds.spotifycdn.com/index.json` 查询 `macosarm64` 当前 stable minimal → 校验 SHA-1 → 构建 `libcef_dll_wrapper` → 编译 Helper → 编译 SwiftUI 主程序 → 嵌入 Framework/Helper → ad-hoc 本机签名 → `codesign --verify --deep --strict` → 输出桌面 `快剪.app`。

Google Sheets 链接识别不依赖页面 DOM：当前 Sheet 使用官方 CSV 导出地址读取，因此右侧行号来自实际 CSV 行序；网页区域只负责浏览。这避免 Google Sheets 页面内部 DOM 变化导致“识别链接”按钮失效。
