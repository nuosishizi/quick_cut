# 快剪 QuickCut 2.7.20

`because the third one` 里 `the` 和 `third` 会同时变黄。

## 根因

Fusion 的 Character Level Styling 关键帧会在两段之间插值。只给当前词写黄色时，前一词的区间还没卸掉、后一词已经叠上来，两段范围一起亮。

## 修法

按 AutoSubs `ApplyHighlight`：每一帧给全句每个词写死填色（当前词高亮，其余词原色），并在下一状态前一帧锁住同一份数组，插值窗口最多 1 帧。整句底单独放在 `快剪底` Text+ 上，不挂 CLS，避免再被切成一词一块。
