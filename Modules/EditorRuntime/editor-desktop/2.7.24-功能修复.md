# 快剪 QuickCut 2.7.24

剪刀不能直接剪：默认选中的是 `video/main`，不是时间线上的真实片段，切割和 Q/W 经常提示“先选择片段”。

## 按达芬奇改

- ✂ / ⌘B / ⌘\：播放头切过的片段都切（主音画、叠加、图片、文字、字幕），不用先点中某一段
- B / 剃刀：点片段中部就切开，成组音画一起切
- Q / W：播放头在哪段上就剪哪段，不必先点选
- 拖主轨两端：裁掉的部分会波纹前移叠加轨道，字幕只平移不吞
- Delete 不会再因为选中 `main` 整段清掉主视频
- 字幕切开时词级时间戳留在左右两半
