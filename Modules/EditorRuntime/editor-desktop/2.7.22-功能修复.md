# 快剪 QuickCut 2.7.22

2 行模式的字幕，快剪预览显示成 3 行（如 Proverbs 12 / :22: "Lying lips / are），发送到达芬奇仍是 2 行。

## 根因

预览换行比达芬奇窄一截（先减背景 padding 再乘 0.94），又没有把行数卡在 2。同一句被提前拆成三截。

## 修法

预览和 `wrapCaptionText` 用同一套宽度。2 行模式最多一个换行点，和达芬奇一致。
