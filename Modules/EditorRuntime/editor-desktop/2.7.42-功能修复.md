# 快剪 QuickCut 2.7.42

1. **AI 降噪与全轨母带处理异常修复**
   - 修复整轨应用 AI 降噪（`applyDenoiseToTrack`）与还原原始音频（`revertDenoisedTrack`）时触发的 `syncAudioInspectors is not defined` 报错。
   - 更正为调用标准属性同步方法 `syncAudioControls()`，确保降噪完成后音频属性面板和预览播放状态无缝刷新。
   - 注入全局安全别名 `window.syncAudioInspectors = syncAudioControls`，杜绝任何历史缓存或遗留调用触发异常。

2. **体检卡片排序与 AI 已处理分组优化**
   - 体检卡片按时间戳线性排序，AI 已处理项分组半透明显示并提供覆盖操作按钮。
   - 新增「保留为独立字幕」按钮，读法差异/口播多读/重读卡壳可自成字幕不合并。

3. **口播多读与上下文回显**
   - 优化口播多读/残句的文案上下文回显，展示完整原句与逐词差异对照。

4. **版本全量自动化回归测试**
   - 标点断行、经文排版、波形非破坏性修剪、达芬奇联动、AI 语音母带与降噪全链路等 187 项核心自动化测试全量通过。
