# 快剪 QuickCut 2.7.15

时间线又变成一排橙色碎条。逐条 `InsertFusionTitleIntoTimeline` 仍会互相挤压。

## 修法

按 AutoSubs 的方式做快剪自己的母版：媒体池生成器 `快剪Text+`（普通 Text+，不是 AutoSubs 宏），打成 `caption-bin.drb`，`AppendToTimeline` 按语音帧铺条。不再把逐条插入当主路径。字幕起点跟时间线上第一段画面素材对齐。
