# 快剪 QuickCut 2.7.35

本版相对 2.7.34：修复 Windows 默认 GPU 导出报 `spawn ENAMETOOLONG`。

- 有英伟达显卡、默认勾 GPU 时，逐词字幕 PNG 不再全部写进 FFmpeg 命令行。
- 字幕叠层改由滤镜脚本 `movie=` 加载；命令太长时自动写成 `-filter_complex_script` 再启动。
- 若仍超长会自动重试，并给出中文提示，可继续用 GPU 导出。
