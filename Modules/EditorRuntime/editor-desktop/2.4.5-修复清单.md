# 快剪 QuickCut 2.4.5

- 移除首次启动时强制复制并覆盖 `~/Applications/快剪.app` 的逻辑。
- 不再因为旧版同名 APP、复制冲突或 App Translocation 而静默退出。
- 首次启动把运行组件和本地原生模块复制到独立的版本缓存目录。
- FFmpeg、FFprobe、Bun 与原生模块均从非隔离目录运行，修复 `EBADMACHO`。
- 运行组件不完整或准备失败时显示明确错误提示。
- 保留 2.4.4 的编辑器、工程和媒体功能，不重置用户工程数据。
