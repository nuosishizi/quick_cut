# 快剪 QuickCut 2.6.2

- 根据用户提供的 `startup-error.log` 确认：2.6.1 已完成运行组件复制，但 Bun 在加载编辑器时无日志异常退出。
- 恢复此前已验证可启动、并与当前 WebView 原生模块匹配的完整 Bun ARM64 组件。
- 2.6.2 使用新的运行缓存目录，不会复用 2.6.1 的错误运行组件。
- 安装器、启动器与自动测试均固定校验完整组件的实际大小和 SHA-256。
