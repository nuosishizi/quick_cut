# 快剪 QuickCut 2.7.33

本版相对 2.7.32：全局整理收尾、框选删除、导出设备和工程删除。

- 全局整理接上气口体检、备注清理（含 `([CTA])`）、口语并进绿字幕；经文只锁定不自动切。
- 点错「关闭」后可用「上次整理结果」再切勾选气口，不用重新识别。
- 框选只选中，不自动切开；删除只动选中范围内的片段。
- `number one` 误读在 `point one` 前会被当成废读前缀，不写进绿字幕。
- 导出可勾选 GPU（默认）或 CPU 软件编码。
- 首页删除工程确认框能看见；Windows 占用目录时重试搬进回收目录，不再直接报 EPERM。
