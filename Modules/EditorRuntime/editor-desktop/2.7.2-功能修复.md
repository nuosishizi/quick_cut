# 快剪 QuickCut 2.7.2

- 字幕预览、样式卡片与成片统一使用外描边语义。
- 滤镜预览照片通过应用内部资源服务器载入，安装后不再出现空白缩略图。
- 美颜 GPU 预览失败时自动切换到可见的实时回退效果，所有滑块都会立即改变画面。
- 删除视频时间段时同步裁掉字幕词句、待处理项和问题标记，并重算后续时间戳。
- “按当前断行替换”优先使用当前剪辑后仍存在的逐词文案映射，不会恢复已经删除的词句。
- 导出移除极慢的双重邻域磨皮，使用快速时空保边降噪配合硬件编码；准备阶段即显示进度，并兼容多种 FFmpeg 进度字段。
