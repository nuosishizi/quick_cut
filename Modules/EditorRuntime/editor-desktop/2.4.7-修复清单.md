# 快剪 QuickCut 2.4.7

- 修复 `Contents/MacOS/QuickCut` 错误地从 `Contents/MacOS/runtime` 查找运行组件；现在统一定位到 `Contents/Resources`。
- 按 WebView 0.4.1 的启动要求，等待 macOS 图形事件循环就绪后再创建菜单、窗口和 WebView，避免启动后立即退出。
- 修正菜单角色 `selectAll` 为组件支持的 `selectall`，避免初始化菜单时直接抛错退出。
- Bun、FFmpeg、FFprobe、编辑器源码和原生 WebView 模块继续复制到独立的 2.4.7 运行目录，避免 App Translocation 与 `EBADMACHO`。
- 启动失败不再静默退出；完整输出写入 `~/Library/Application Support/QuickCut/startup-error.log` 并显示错误提示。
- 安装程序会备份旧版 APP，不删除工程、素材、设置或工程数据库。
