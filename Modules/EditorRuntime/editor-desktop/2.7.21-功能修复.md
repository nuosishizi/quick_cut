# 快剪 QuickCut 2.7.21

个别字幕字周围有一条红/品红描边，同一条样式里其他句子没有。

## 根因

AutoSubs 母版 Element 2 默认是品红描边（Red=1, Green≈0.39）。上一版只要开了高亮就顺手打开描边，个别片段没盖掉母版颜色，红边就露出来。

## 修法

写样式前先 `Enabled2=0` 清掉母版描边。只有快剪里描边大于 0 才再画，并且用你设的描边色。
